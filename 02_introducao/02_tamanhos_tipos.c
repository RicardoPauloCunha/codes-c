#include <stdio.h>

int main_old2() {
	printf("\nTamanho do tipo int neste compilador = %d", sizeof(int));
	printf("\nTamanho do tipo float neste compilador = %d", sizeof(float));
	printf("\nTamanho do tipo char neste compilador = %d", sizeof(char));

	return 0;
}